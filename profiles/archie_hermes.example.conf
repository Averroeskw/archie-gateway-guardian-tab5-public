# Copy to the SD card as archie_profile.conf, replace placeholders, import once,
# then delete it. Never commit a filled-in profile.
connection_mode=hermes
gateway_ws_url=ws://<trusted-hermes-host>:8787/ws/tab5
gateway_token=<shared-device-token>
